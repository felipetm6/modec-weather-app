import styled from 'styled-components';

const Wrapper = styled.footer`
  color: #808ca4;

  display: flex;
  justify-content: center;
  padding: 8px 36px;
`;

export const FooterStyled = { Wrapper };
